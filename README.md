# Aptus CSS

[Arriminum](https://arriminum.com) · [github.com/arriminum](https://github.com/arriminum)

Aptus CSS is a lightweight, reusable CSS library that provides the common visual base for Arriminum's sites and web applications: reset, grid, structure, images, forms, common UI components, and utility classes.

It descends from the earlier `rimini.css` library and was renamed **Aptus** to be clearer, framework-independent, and semantically appropriate for a shared styles library.

## Features

- Reset and normalization for consistent cross-browser rendering.
- A simple row/column grid (`.g-row`, `.col1`–`.col12`).
- Reusable utility classes for spacing, alignment, display, and visibility.
- Common UI patterns: buttons, message bars, breadcrumbs, eyebrow labels, status badges, spinners, and fade effects.
- Responsive image helpers and small visual treatments such as shadows, rounded images, captions, and hover effects.
- No dependency on Bootstrap, Tailwind, or any other CSS framework.

Aptus does not aim to replace a full framework. It exists to avoid re-implementing the same structures on every project, leaving each site to define only its own visual identity, unique components, and page-specific adjustments.

## Installation

Aptus is distributed as compiled CSS, not as a package on a public registry. Consuming projects should link the built files rather than importing files from `src/` directly.

```html
<link rel="stylesheet" href="/lib/css/aptus/3.0.0/reset.min.css">
<link rel="stylesheet" href="/lib/css/aptus/3.0.0/grid.min.css">
<link rel="stylesheet" href="/lib/css/aptus/3.0.0/commons.min.css">
```

Each module is built and minified separately, so a project can include only the modules it needs. See [docs/requirements.md](docs/requirements.md) for the full list of modules and how they are built.

## Quick start

```html
<div class="g-row">
  <div class="col6">
    First column content
  </div>

  <div class="col6">
    Second column content
  </div>
</div>
```

## Project structure

```text
src/            Source CSS modules and version/name files
dist/           Compiled output (generated by the build, not edited by hand)
docs/           Requirements, conventions, and usage documentation
docs/examples/  Living HTML examples for each CSS module
rpack.json      Asset packer configuration (CSS minification)
rimi.json       Build and local deploy configuration
CHANGELOG.md    Notable changes, aligned with src/VERSION
```

## Documentation

- [docs/requirements.md](docs/requirements.md) — supported browsers, build tools, and technical requirements.
- [docs/conventions.md](docs/conventions.md) — naming, structure, and contribution rules.
- [docs/usage.md](docs/usage.md) — class reference with examples.
- [docs/examples/](docs/examples/) — standalone HTML pages demonstrating each module.

Agents and contributors working in this repository should also read [AGENTS.md](AGENTS.md).

## Development

Aptus is built with `rpack` (CSS minification) and `rimi` (build and local deploy), both external CLI tools available in the development environment, not npm packages.

```bash
rpack build     # minify src/*.css into dist/*.min.css
rimi build      # copy VERSION/APP_NAME, run rpack build
rimi deploy     # build and install into the local CSS lib path
```

Edit only files under `src/`. Files in `dist/` are generated and must not be edited by hand. See [docs/requirements.md](docs/requirements.md) for details on the build pipeline.

## Versioning

Aptus follows [Semantic Versioning](docs/versioning.md) (MAJOR.MINOR.PATCH). The current version is the single line in [src/VERSION](src/VERSION). Version bumps are requested explicitly; see [docs/versioning.md](docs/versioning.md) for details on when to bump and how to request one. Every bump is recorded in [CHANGELOG.md](CHANGELOG.md) with the changes that triggered it.

## Contributing

- Repository: [github.com/arriminum/aptus](https://github.com/arriminum/aptus)
- Read [docs/conventions.md](docs/conventions.md) before adding or changing a class.
- Check `docs/usage.md` and `docs/examples/` for an existing equivalent before creating something new.
- Keep changes small, objective, and traceable; avoid broad refactors without a clear need.
- Update `CHANGELOG.md`, `src/VERSION`, and the relevant docs together with any public change.

## License

[MIT](LICENSE) © [Arriminum](https://arriminum.com)
